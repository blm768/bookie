<% content_for :heading, 'New user' %>

<%= form_for @web_user do |f| %>
  <%= form_errors(@web_user.errors) %>
  <%= f.label :email %></br>
  <%= f.email_field :email %><br/>
  <%= f.submit %>
<% end %>
